<% if(is_home()) { %>
<header class="header index-header">
<% } else { %>
<header class="header">
<% } %>
    <!-- top read progress line -->
    <div class="read-progress"></div>
    <!-- sidebar menu button -->
    <div class="header-sidebar-menu"><i class="fas fa-bars"></i></div>
    <!-- toggle banner for post layout -->
    <% if (is_post()) { %>
    <div class="banner">
        <div class="blog-title">
            <a href="<%- url_for() %>" ><%= theme.main_title || config.title%></a>
        </div>
        <div class="post-title">
            <a href="#" class="post-name"><%- page.title %></a>
        </div>
    </div>
    <% } %>
    <!-- back to home page text -->
    <a class="home-link" href=<%- config.root %>><%- theme.main_title || config.title %></a>
</header>